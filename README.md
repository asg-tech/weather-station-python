THIS IS THE README FOR THE UDP LISTENER

This code converts Glen's enum.ts and server.ts files to Python Code.

Files written are udpclasses.py (in lieu of enum.ts) and udplistener.py (in lieu of server.ts)